// TODO : fichier Kotlin d'origine (RacialAbility.kt) non fourni -> version minimale à confirmer.
export interface RacialAbility {
  readonly id: number
  readonly name: string
  readonly description: string
}
